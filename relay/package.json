{
  "name": "bgpromidi-relay",
  "version": "1.0.0",
  "description": "Tiny WebSocket relay for BG-ProMIDI's live-viewer link. Forwards a broadcaster's decoded counter state to any viewers watching the same session code. No database, no auth beyond the session code itself -- treat a session code like a shareable link.",
  "private": true,
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
